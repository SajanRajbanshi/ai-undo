{
  "$comment": "Highlighting only: delegates entirely to source.css.less. Exists so the patch view can use a language no server registers for.",
  "scopeName": "source.lfct.less",
  "patterns": [
    {
      "include": "source.css.less"
    }
  ]
}
