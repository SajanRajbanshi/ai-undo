{
  "$comment": "Highlighting only: delegates entirely to text.html.derivative. Exists so the patch view can use a language no server registers for.",
  "scopeName": "source.lfct.html",
  "patterns": [
    {
      "include": "text.html.derivative"
    }
  ]
}
